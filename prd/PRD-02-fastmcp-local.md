# PRD 02 - FastMCP Local Protocol

Implement strict peer tools, envelopes, idempotency, deadlines, and explicit state
transitions that work between separate localhost processes before public deployment.

